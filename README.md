# Proyecto-3-MT


## needed binaries 
- pypy3
- any python pkg manager (or pkg manager that can take care of python dependecies)
- :D (nothing else)

## How to run:
```bash
pypy3 -O "src/main.py" --tm_config_file "files/turing_machine_config.yaml" --validator_mode "true"
```
```
